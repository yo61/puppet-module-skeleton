# == Class <%= metadata.name %>::service
#
# This class is meant to be called from <%= metadata.name %>
# It ensures the service is in the desired state
#
class <%= metadata.name %>::service(

  assert_private()

  $service_name = $::<%= metadata.name %>::service_name
  $service_ensure = $::<%= metadata.name %>::service_ensure
  $service_enable = $::<%= metadata.name %>::service_enable
  $service_hasstatus = $::<%= metadata.name %>::service_hasstatus
  $service_hasrestart = $::<%= metadata.name %>::service_hasrestart

  service { $service_name:
    ensure     => $service_ensure,
    enable     => $service_enable,
    hasstatus  => $service_hasstatus,
    hasrestart => $servive_hasrestart,
  }

}
