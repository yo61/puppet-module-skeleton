# == Class <%= metadata.name %>::install
#
class <%= metadata.name %>::install(
  $version,
) {

  include ::<%= metadata.name %>::params
  package{$::<%= metadata.name %>::params::package_name:
    ensure => $version,
  }

}