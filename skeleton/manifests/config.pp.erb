# == Class: <%= metadata.name %>::config
#
# This class is called from <%= metadata.name %> for service config.
#
# The parameters are described there
#
class <%= metadata.name %>::config {

  assert_private()

  include ::<%= metadata.name %>::params
  $some_param = $::<%= metadata.name %>::some_param
  $some_param_with_default_value = $::<%= metadata.name %>::some_param_with_default_value
  }

}
